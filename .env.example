# Vite environment variables
# If not set, the app falls back to same-origin "/api".
# You can set either the API origin or the full /api URL.
# Examples:
#   VITE_API_URL=http://localhost:5000
#   VITE_API_URL=http://localhost:5000/api
VITE_API_URL=http://localhost:5000
